import {
  PUBLIC_UI_ORIGIN,
  PUBLIC_SERVER_ORIGIN,
  PUBLIC_LIVE_ORIGIN,
  PUBLIC_ETH_NODE_ORIGIN
} from "./location-orgins";

import {
  indexHTML
} from "./html";


export {
  indexHTML,

  PUBLIC_UI_ORIGIN,
  PUBLIC_SERVER_ORIGIN,
  PUBLIC_LIVE_ORIGIN,
  PUBLIC_ETH_NODE_ORIGIN
}
