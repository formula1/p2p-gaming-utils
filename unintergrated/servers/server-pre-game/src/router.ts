import {
  Router
} from "express";

const router = Router();

router.post("create", (req, res, next)=>{

})

router.get("list", (req, res, next)=>{

})

router.get("enter/:id", (req, res, next)=>{

})


router.get("delete/:id", (req, res, next)=>{

})
