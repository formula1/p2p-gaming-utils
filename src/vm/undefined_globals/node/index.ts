// import {
//   BROWSER_LISTENERS
// } from "./listeners";
import {
  NODE_METHODS
} from "./methods";
import {
  NODE_VALUES
} from "./values";

export const NODE_GLOBALS = [].concat(
  NODE_METHODS
).concat(
  NODE_VALUES
);
